// The admin is a client-rendered SPA: the JWT lives in localStorage and every
// request is authenticated from the browser, so server-side rendering is off.
export const ssr = false;
export const prerender = false;

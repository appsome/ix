# Houdini page query for the {{.NamePascal}} list route ({{.Module}}).
# Generated by `ix add entity --name {{.Name}} --frontend`. Edit fields freely,
# then run `ix generate houdini` to refresh the typed client.
query {{.NamePluralPascal}}List {
  {{.NamePluralCamel}}(first: 50, offset: 0) {
    id
    name
  }
}

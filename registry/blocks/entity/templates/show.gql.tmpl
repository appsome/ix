# Houdini page query for the {{.NamePascal}} show/edit route ({{.Module}}).
# Generated by `ix add entity --name {{.Name}} --frontend`. $id is filled from
# the [id] route param automatically (houdini-svelte matches names). Edit
# fields freely, then run `ix generate houdini` to refresh the typed client.
query {{.NamePascal}}Detail($id: ID!) {
  {{.NameCamel}}(id: $id) {
    id
    name
  }
}
